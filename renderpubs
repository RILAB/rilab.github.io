#!/bin/zsh

set -euo pipefail

repo_root="${0:A:h}"
cd "$repo_root"

Rscript scripts/manage_pubs.R render

echo "Rendered $repo_root/pubs.html from doi.txt using data/pubs_cache.json and data/pubs_overrides.yml"
